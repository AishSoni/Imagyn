# Imagyn MCP Server Dependencies
# Requires Python 3.10+ due to MCP library requirements

# Core dependencies
mcp>=1.0.0
httpx>=0.25.0
aiofiles>=23.0.0
pillow>=10.0.0
pydantic>=2.0.0
python-multipart>=0.0.6
anyio>=4.0.0
websockets>=11.0.0
replicate>=1.0.0

# Development dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0
mypy>=1.0.0
